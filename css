button {
    position: absolute;
    top: 30%;      /* 화면 세로 기준, 약간 위쪽 */
    left: 10%;     /* 화면 왼쪽에서 10% 위치 */
    padding: 12px 24px;
    border: none;
    border-radius: 8px;
    background: #d9534f;
    color: #fff;
    font-size: 18px;
    cursor: pointer;
    box-shadow: 0 4px 8px rgba(0,0,0,0.2);
  }
  
  button:hover {
    background: #c9302c;
  }